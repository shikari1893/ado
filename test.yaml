trigger: none

pool:
  vmImage: ubuntu-latest
  
resources:
  repositories:
  - repository: test1 # The name used to reference this repository in the checkout step
    type: github
    endpoint: shivgithub
    name: shikari1893/ado
    #ref: refs/heads/main
    trigger:
     branches:
      include:
        - main
      
    

stages:
- stage: CheckoutCode
  jobs:
  - job: script
    steps:  # checkout code in the CLIIP repository
    - bash: |
              pwd
              ls -l
              
